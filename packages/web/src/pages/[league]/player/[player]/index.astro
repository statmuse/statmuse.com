---
import PlayerProfile from '@components/player-profile.astro'
import { Caching } from '@lib/caching'
import type { LeagueParam } from '@lib/params'

const player = Astro.params.player as string
const leagueParam = Astro.params.league as LeagueParam

Caching.swr(Astro)
---

<PlayerProfile league={leagueParam} {player} />
