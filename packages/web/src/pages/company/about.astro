---
import Layout from '@layouts/base.astro'
import { Caching } from '@lib/caching'

// export const prerender = true

const metrics = [
  { quantity: '350M+', unit: 'Searches' },
  { quantity: '4K+', unit: 'Original illustrations' },
  { quantity: '300+', unit: 'Muse accounts' },
]

const affiliations = ['nflpa', 'google', 'disney']

Caching.swr(Astro)
---

<Layout
  meta={{
    title: 'About',
    description:
      'StatMuse, a creative technology company, is the leader in conversational AI for sports. The company was founded in 2014 by two lifelong friends and is backed by Disney, Google, Techstars Ventures, Greycroft, the NFL Players Association and NBA Commissioner Emeritus, David Stern.',
  }}
  colors={{ foreground: '#fff', background: '#00c1d8' }}
  enableAdBanner={false}
  hero={{ content: 'The elephant of sports and financial information.' }}
>
  <div
    class="py-12 md:py-16 w-full md:w-[50vw] mx-auto md:text-xl space-y-[1em]"
  >
    <h2 class="md:text-2xl text-[#333] font-semibold">About Us</h2>
    <p>
      StatMuse, an artificial intelligence company, is the leader in
      conversational search for sports stats and information.
    </p>
    <p>
      Search stats, scores, schedules, standings, odds, fantasy, bios, recaps
      and more — from live games all the way back to the inaugural seasons — for
      the NBA, NFL, NHL, MLB and PGA.
    </p>
    <p>
      StatMuse Search provides instant answers to trillions of sports questions,
      while turning every result into a piece of personalized content on all
      your favorite devices.
    </p>
    <p>
      The company was founded in 2014 and is backed by Disney, Google, Techstars
      Ventures, Greycroft, the NFL Players Association, Russell Okung and Tracy
      McGrady.
    </p>
  </div>
  <div class="w-full md:w-[50vw] text-center mx-auto pb-[72px] md:pb-[120px]">
    <h2 class="text-[#777] font-semibold mb-6">
      <span class="overview-quote-body md:text-3xl mr-[1.5em]">
        Sports is, by itself, the international language, but StatMuse has taken
        a giant leap forward, transforming sports statistics into meaningful,
        storytelling prose.
      </span>
      <span class="opacity-50 text-sm md:text-base whitespace-nowrap">
        - David J. Stern, NBA Commissioner 1984-2014
      </span>
    </h2>
  </div>
  <ul class="flex items-center justify-center flex-wrap">
    {
      metrics.map((metric) => (
        <li class="inline-block text-center w-full md:w-auto m-3 md:m-6">
          <h3 class="text-[2rem] md:text-[3.25rem] font-semibold text-[#777] whitespace-nowrap">
            {metric.quantity}
          </h3>
          <h5 class="text-sm md:text-base font-semibold text-[#777] whitespace-nowrap">
            {metric.unit}
          </h5>
        </li>
      ))
    }
  </ul>
  <h2
    class="text-[#333] text-center font-semibold text-base md:text-[1.625rem] mb-0 md:mb-3 mt-9"
  >
    Strategic Investors
  </h2>
  <ul class="site-container flex items-center justify-center flex-wrap">
    {
      affiliations.map((affiliation) => (
        <li
          class={`inline-block w-1/2 md:w-40 h-12 md:h-16 mt-8 m-3 md:m-6 bg-contain bg-center bg-no-repeat overview-affiliation-${affiliation}`}
        />
      ))
    }
  </ul>
</Layout>
